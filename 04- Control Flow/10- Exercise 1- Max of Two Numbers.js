/**Max of Two Numbers
 * Write a function that take two number
 * and return the maximum of the two
 */

function Max(num1, num2) {
  return num1 > num2 ? num1 : num2;
}

let number = Max(5, 10);
console.log(number);
