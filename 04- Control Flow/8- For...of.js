/** for of
 * new way to iterate over array elements
 * Definition
 * for(let element of elements)
 */

// iterate over array elements
const colors = ["red", "green", "blue"];
for (let color of colors) {
  console.log(color);
}
