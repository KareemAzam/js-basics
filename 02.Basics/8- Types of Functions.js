/** Reference Types :
 * 1. Object
 * 2. Array
 * 3. Function
 */

/** 3. Function Types
 * Performing a task : void
 * calculating a value : return value
 */

// * Performing a task
function great(name, lastName) {
  console.log("Hello" + " " + name + " " + lastName);
}

// * calculating a value
function square(number) {
  return number * number;
}

console.log(square(2));
